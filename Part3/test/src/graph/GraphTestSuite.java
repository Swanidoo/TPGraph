package graph;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({
    VertexTest.class,
    EdgeTest.class,
    IncidenceArrayGraphTest.class
})
public class GraphTestSuite {
    // Cette classe reste vide
    // Elle sert uniquement à regrouper les tests
}